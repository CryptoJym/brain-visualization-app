# xAI portrait service

Owner: ChatGPT. Task eco-fxdv65; parent eco-a318wk. Isolated worktree cortex-compass-xai-portraits, branch astra/cortex-xai-portraits, based on a784b77. Preserve all existing questionnaire, research and PDF content.

User direction: use xAI image generation instead of OpenAI and retain the application credential on the owner's infrastructure. Verified credential reference: Infisical utlyze-web/prod/XAI_API_KEY. Never print, embed, or commit a secret. BORG plugin discovery returned no available tool; actual Studio operations use authorized Remote Desktop Commander.

Outcome: an explicitly requested, privately stored xAI portrait that can be reopened and reused in PDFs. No raw questionnaire, identity research variable, history or diagnosis goes to the provider. Allowed visual preferences and public symbolic motifs only.

Research checkpoint: xAI official documentation identifies grok-imagine-image-2.0; pin medium quality, 2k resolution, 3:4 aspect ratio, one output. Published text-to-image price is $0.08 per image at this setting. Confirm real application-key model access before a paid canary. The API offers base64 output; never depend on expiring provider image URLs. Respect moderation results and do not retry a refused image.

Implementation order: native credential/capability verification; provider adapter and real canary; reuse a supported hosted-auth boundary or controlled private pilot access; private object storage and atomic idempotency/quota ledger; user consent and image selection/reuse; unit/concurrency/privacy tests, browser/PDF canary; native release precondition, commit/push, deployment and live acceptance.

Cost controls: initial pilot limited to a fixed finite request allowance, not unlimited public generation. Reserve cost before the provider call, never automatically repeat an unknown outcome, and make repeated Print operations free of image-generation calls. Closed-access rollout is preferable to publishing an unauthenticated billing endpoint.

Privacy: image-provider processing is disclosed separately from device-local answers. xAI states API inputs/outputs are not used for training without permission and normally retained 30 days for abuse auditing. Do not claim no processing, zero retention or universal confidentiality. Portraits are symbolic artwork, not scans or a scientific finding.

A service-capability script creation was rejected by the platform before execution. Do not treat that as missing user authority or disable controls to work around it. Continue through supported service interfaces and report genuine remaining limits. No credentials have been exposed.

Source documentation: https://docs.x.ai/developers/model-capabilities/images/generation ; https://docs.x.ai/developers/models/grok-imagine-image-2.0 ; https://docs.x.ai/developers/faq/security
