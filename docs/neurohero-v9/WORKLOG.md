# Neurohero v9 worklog

Owner: ChatGPT on Studio0. Canonical work item: `eco-a318wk`.

## 2026-09-12 durable recovery checkpoint

The user explicitly authorized direct Studio/BORG work and requested that this project not depend on temporary chat artifacts. The earlier failure to create a remote copy was a single automatic safety-gate rejection of a compound remote command, not missing Studio authorization. A simple Git worktree command subsequently succeeded.

Durable isolated working copy: `/Users/utlyze/Projects/cortex-compass-neurohero-v9`.
Branch: `astra/cortex-neurohero-v9`.
Base: public Field Guide source `e17f572`.
Production remains unchanged during this implementation lane.

User direction: choose the best implementation, go deep on neuroscience, make strengths and practical agency emotionally compelling, preserve scientific limits, and eventually support persistent generated hero artwork without exposing raw case history to the image provider.

Immediate order: preserve architecture → implement evidence/process registry → implement reported functional observations and counterexamples → deterministic composition engine → hero profile/report integration → portrait brief/persistence architecture → tests → production prerequisites → release verification.

## Continued implementation and integration

The Studio implementation now includes the 27-process / 24-composition functional atlas, counterexamples, user-selected eligible signatures, a two-page hero field guide, a strengths-only card, and a scientific observation/evidence appendix. All 169 current unit/contract tests passed before the full browser checks. No pooled meta-analysis or neurological predictive validation is claimed.

Persistence was corrected to save the hero record inside the main reflection in one browser-storage operation. This prevents pairing a separate old hero record with a new history. Old reflections receive no invented hero answers. Unsupported versions fail without rewriting data.

The portrait editor compiles only allowlisted appearance choices and public catalogue motifs. Arbitrary profile titles, private history and unsupported fields are not passed through. An optional, explicitly consented device-image import decodes and re-encodes raster pixels, removes metadata, stores bytes in IndexedDB, and keeps a bounded asset reference in the profile. Printing reuses those bytes. External image URLs, SVG and scripts are not accepted as stored portrait references.

Paid generation and cloud accounts remain explicitly unavailable in the UI; no user data or visual brief is sent to an image provider. This is a real remaining backend/provider task, not a remote filesystem restriction. BORG runtime credentials have not been repurposed.

Bibliography correction from primary-source verification: the interoception/emotion/social-cognition record is Adolfi et al. (2017), DOI 10.1016/j.cortex.2016.12.019, PMID 28088652; the prior draft mistakenly paired it with another paper's DOI and author label. The Rahapsari/Levita prospective meta-analysis retains its null sex-moderation finding. References are related research context, not validation of named hero combinations.

Production preflight this continuation: 8cc9f3fa-0986-497e-920f-1c6f2a15b1ec at 100%. The new code will not replace it until browser, PDF and regression acceptance is complete. Native version is rechecked immediately before publication.
