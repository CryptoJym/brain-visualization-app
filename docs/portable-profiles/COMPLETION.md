# Portable profiles released and verified

Live Worker: 6fc5170e-fbea-44f1-bfaf-07e0b6d7fec0 at 100% traffic. Native publication time: 2026-09-13T04:14:56.582130Z. Deployed source: df725752a7af85bda77f23073780541e4dea66fe on astra/cortex-portable-profiles. Origin: https://cortexcompass.utlyze.com.

The final integrated build passed 235 unit tests. Live checks: 21 portrait-edition, 30 backup/restore, 38 hero, 11 hero WebKit, 51 report/mobile, 40 developmental-flow and 43 branding checks: 234 passed. The final passing hero evidence is review-live-hero-cwd/verification.json. Earlier timeout receipts remain available; a direct review invocation used the wrong working directory for its file fixture. The correct-directory run passed without weakening assertions.

Seven live assets, including HTML, CSS, JavaScript bundles, manifest and favicon, matched the verified build by SHA-256. The public portrait-status endpoint still reports the configured xAI private pilot. No provider backend, account, credential, quota or scientific inference model was changed.

The backup suite downloaded an actual encrypted file, previewed and opened it in a separate Chromium browser profile, and decrypted/restored it in WebKit. Portrait bytes and their hash were preserved. Existing saved answers remained unchanged until a separate explicit Save profile action. The restored two-page report was rendered and visually inspected.

Portrait-edition tests used simulated workspace responses in the live application, not paid provider generation. Saved images remain usable at zero allowance, new editions require explicit consent, differing artwork requires acknowledgement, and late responses do not restore signed-out state. No new paid image request occurred.

This is file portability, not self-service cloud accounts. A forgotten backup password cannot be reset by Cortex Compass. WebKit/iPhone emulation is not a physical-iPhone print certification. Evidence is retained under .local-evidence/portable-profiles.
