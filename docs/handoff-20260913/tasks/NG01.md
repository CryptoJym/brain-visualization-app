# NG01 — Reconcile graph checkpoint onto current verified Cortex source

Native ID: `eco-eib7d3`. Status: open. Parent: `eco-cq76kh`.

Role: integration engineer. Depends on: none.

Scope: new isolated worktree, graph-only checkpoint aae0e60.

Start from the latest verified hero-editions branch, not the old 43ceccb graph base. Import only graph paths, retain original history and keep runtime integration off until accepted. Confirm active ownership and native release version.

## Acceptance
- Current production UI, required Male/Female control, backup and xAI service are preserved.
- Graph checkpoint is present on a new named branch with clean ownership.
- Existing regression baseline and nine prototype tests are recorded without calling them scientific validation.

Native acceptance readback: Current production UI, required Male/Female control, encrypted backup and xAI service are preserved. Graph checkpoint aae0e60 is imported into a new branch with clean ownership. Baseline regression and prototype tests are recorded separately from scientific validation.
