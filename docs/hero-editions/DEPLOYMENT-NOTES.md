# Required Male/Female field and full hero-edition publication

Owner: current ChatGPT continuation. Canonical task: eco-iln18c. Existing Studio branch: astra/cortex-hero-editions-20260913. The user explicitly requires this complete release to be live.

The biological-sex control has exactly two selectable values: Male and Female. Neither Not sure nor Prefer not to answer is offered. It starts unselected, with a disabled hidden prompt rather than an invented default. The continue action is disabled until one of the two is selected. This applies only to the biological-sex field; other history questions retain their existing response semantics.

Context version: cc-context-2.1. Older withheld, uncertain or unlisted sex responses are preserved as archival values, never converted into Male/Female. An old identity response cannot supply a sex. Archived values are not new choices in the current form. Saved profiles, encrypted backups and chosen portraits remain compatible.

Publication scope includes all cinematic report/site code, the landing hero showcase, five report palettes, ink-saving mode, logo/favicon and report assets. Personal data is not being sent to Gamma or an image provider. Existing xAI service, private storage, quotas and credentials are unchanged. Unfinished neurograph work in its separate worktree is not part of this release.

Acceptance: 253 unit/contract tests and 281 local browser checks, including required selection on Chromium and WebKit, then native deployment and public verification. Sample reports are regenerated from context v2.1 before publication. A4/Letter exports retain the approved image without paid regeneration.

Native predecessor confirmed before publication: 6fc5170e-fbea-44f1-bfaf-07e0b6d7fec0. Preserve this version as rollback. Do not delete or recreate bound storage. If the predecessor changes, reconcile that release rather than overwrite it.
