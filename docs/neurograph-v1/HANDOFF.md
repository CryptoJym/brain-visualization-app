# Neurocomposition graph: preserved research prototype, not a live release

This worktree contains the unfinished graph implementation from the Cortex Compass thread. Production does not import these modules. Its base is the older portable-profile commit 43ceccb; do not deploy this worktree over the newer cinematic/required-sex release.

Canonical graph work item: `eco-cq76kh`. Program work item: `eco-a318wk`. Current live source is on `astra/cortex-hero-editions-20260913`; start with that worktree's `README.md` and `docs/handoff-20260913/README.md` for the complete cross-branch handoff.

Files preserved: five data modules, seven engine modules, the original plan, and nine prototype tests. The standalone graph suite passed 9/9 on 2026-09-13. This proves only the stated software checks, not scientific calibration, valid source-to-claim mappings, or integration.

Release blockers: hand-assigned numerical evidence/interaction weights are uncalibrated; a shared citation or existing editorial hero name is not evidence of a measured interaction; provenance normalization drops relevant measurement fields; system mappings require reconciliation; the separate processes registry is partial and unused. Evidence needs claim-level appraisal and cohort deduplication. Counts of generated pairs are a search space, not validated neuroscience relationships.

First engineering action: preserve this checkpoint, create a successor branch from the latest verified live source, and bring in only these graph paths. Do not overwrite the live sex-field, report-edition, private portrait, or encrypted-backup changes. Then resolve the evidence/measurement contract before connecting ranking to public hero selection.

No new anatomy was measured, no participant-level meta-analysis was completed, and the draft support index is not a probability, effect size, or confidence estimate. Do not relabel its current 0–1 numbers as scientific certainty. The user wants depth and usefulness; that requires traceable evidence rather than a large count of automatically generated rules.
